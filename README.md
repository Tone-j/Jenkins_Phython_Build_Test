# Jenkins_Phython_Build_Test
Using Jenkins to build and test python code.

### To Run

* `make run`
* follow the input prompts to get the desired output

### To Test

* To run all the unittests: `make test`